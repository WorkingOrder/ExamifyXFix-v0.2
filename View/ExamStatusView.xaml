<UserControl x:Class="ExamifyX.View.ExamStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExamifyX.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <StackPanel>
        <Label Content="Exam Status View"/>
        <Button x:Name="PublishExamBtn" Command="{Binding OpenPublishExamCommand}" Width="70" Height="25" Content="Publish Exams" Margin="0,30,0,0"/>
        <Button x:Name="BackBtn" Command="{Binding BackCommand}" Width="70" Height="25" Content="Back" Margin="0,10,0,0"/>
    </StackPanel>
</UserControl>
